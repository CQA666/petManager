<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pet.petmanager.dao.FosterDao">

    <!-- 结果映射：FosterVO 包含了 Foster 的基础字段以及用户名、房间名称 -->
    <resultMap id="FosterVOMap" type="com.pet.petmanager.VO.FosterVO">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="name" column="name"/>
        <result property="time" column="time"/>
        <result property="days" column="days"/>
        <result property="status" column="status"/>
        <result property="roomId" column="room_id"/>
        <!-- 用户名来自 user 表 -->
        <result property="userName" column="username"/>
        <!-- 房间名来自 room 表 -->
        <result property="roomName" column="roomName"/>
    </resultMap>

    <!-- 多条件分页查询寄养记录，关联 user 表和 room 表 -->
    <select id="selectByPage" resultMap="FosterVOMap">
        SELECT f.*,
        u.username,
        r.name AS roomName
        FROM foster f
        LEFT JOIN user u ON f.user_id = u.id
        LEFT JOIN room r ON f.room_id = r.id
        WHERE 1 = 1
        <if test="status != null and status != ''">
            AND f.status = #{status}
        </if>

        <if test="name != null and name != ''">
            AND f.name LIKE CONCAT('%', #{name}, '%')
        </if>

        <if test="userId != null">
            AND f.user_id = #{userId}
        </if>

        ORDER BY f.id DESC
    </select>

</mapper>